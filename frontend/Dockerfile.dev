FROM node:22-alpine
WORKDIR /app
RUN corepack enable

# Copy lock files + workspace config
COPY pnpm-lock.yaml pnpm-workspace.yaml package.json tsconfig*.json vite.config.ts ./
COPY packages/ packages/
RUN pnpm install --frozen-lockfile

EXPOSE 5174
CMD ["pnpm", "dev"]
